import React from 'react';

const SoichuLogo: React.FC<{ className?: string }> = ({ className = '' }) => (
  <img
    src={`${import.meta.env.BASE_URL}logoSOICHU-01.jpg`}
    alt="Soichu Logo"
    className={`mx-auto w-28 mb-2 ${className}`}
  />
);

export default SoichuLogo;
